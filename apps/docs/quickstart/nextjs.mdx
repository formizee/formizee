---
title: Next.js
description: 'Learn how to ingest your first submission using Next.js'
---

## Prerequisites
To get the most out of this guide, you’ll need to:

- [Have a form created]('https://doc')
- [Get your endpoint ID]('')

Make sure you have the latest version of the Vercel CLI installed.

## Create a Next.js project

<Tabs>
  <Tab title="bun">
  ```bash
    bun create next-app example --yes
    ```
  </Tab>
  <Tab title="pnpm">
  ```bash
    pnpm create next-app example --yes
    ```
  </Tab>
  <Tab title="yarn">
  ```bash
    yarn create next-app example --yes
    ```
  </Tab>
  <Tab title="npm">
  ```bash
    npx create-next-app example --yes
    ```
  </Tab>
</Tabs>

# Update the main page
Update the file under src/app/page.tsx to the following code

<Tab title="page.tsx">
```tsx
export default function Page() {
  const ENDPOINT_ID = "enp_123456";

  return (
    <form method="post" action={`https://api.formizee.com/v1/f/${ENDPOINT_ID}`}>
      <label htmlFor="name">
        Name
        <input type="text" name="name"/>
      </label>
      <label htmlFor="email">
        Email
      <input type="email" name="email"/>
      </label.
      <button>Submit</button>
    </form>
  )
}
```
</Tab>

Now update the
